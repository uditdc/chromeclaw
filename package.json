{
  "name": "chromeclaw",
  "private": true,
  "scripts": {
    "dev": "pnpm -C packages/extension dev",
    "build": "pnpm -C packages/extension build",
    "compile": "pnpm -C packages/extension compile"
  }
}
