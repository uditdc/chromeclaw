{
	"name": "chromeclaw",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"format": "prettier --check .",
		"format:fix": "prettier --write .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"prettier": "^3.8.1"
	}
}
